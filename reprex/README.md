# reprex

One skill: write minimal reproducible examples for R bug reports, GitHub
issues, and questions — lead with the code, `.R` file as the source of truth
(`reprex::reprex()` renders it), minimality as a discipline rather than an
afterthought.

```
/plugin install reprex@njtierney-skills
```
